<resources>

    <style name="Theme.Nesvie_copyZalo" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="android:statusBarColor">@color/primary_blue</item>

        <!-- Icon status bar màu TRẮNG (đồng hồ, wifi, pin sẽ màu trắng) -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar cùng màu -->
        <item name="android:navigationBarColor">@color/primary_blue</item>

        <!-- Màu chính của app -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/primary_blue_dark</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.Nesvie_copyZalo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>